# Configuration for Gemini OCR Service
# Place at /etc/default/gemini-ocr

# --- Paths ---
# Directory containing images to process (Recursive by default)
OCR_ROOT=/mnt/data/ocr_input

# Directory for output files (JSON/TXT)
OCR_OUT_ROOT=/mnt/data/ocr_output

# Path to the Playwright browser profile (must be persistent and logged in)
OCR_PROFILE_DIR=/opt/gemini-ocr/profiles/worker1

# --- OCR Parameters ---
# Prompt ID from gemini_prompts.json (e.g., agad_generic, nurskie_generic)
# OCR_PROMPT_ID=agad_generic

# --- Runtime Control ---
# Limit number of files to process per run (0 = no limit)
OCR_LIMIT=1000

# Recursive scan (1/true or 0/false)
OCR_RECURSIVE=1

# Import only mode (scan and database import, no OCR)
OCR_IMPORT_ONLY=0

# Headed mode (visible browser for debugging)
OCR_HEADED=0
